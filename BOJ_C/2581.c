#include <stdio.h>
#include <math.h>
int main(){
    int min,sum,M,N;
    char field[10000]={0,};
    scanf("%d",&M);
    scanf("%d",&N);
    for(;M!=N;M++){
        for(int i=2;i<;i++){

        }
    }
}